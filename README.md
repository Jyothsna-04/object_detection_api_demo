# Object Detection Drone (Simulation)

This project simulates a **drone with object detection**:
1. Uses YOLOv8 (Ultralytics) with webcam/IP cam.
2. Detects and annotates objects in real time.
3. Sends detection results to a REST API server.

## 🚀 How to Run

### 1. Install dependencies
```bash
pip install -r requirements.txt
